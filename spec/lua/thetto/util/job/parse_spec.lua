local helper = require("thetto.test.helper")
local assert = helper.typed_assert(assert)

describe("parse", function()
  it("joins streamed string", function()
    local concat = require("thetto.util.job.parse").concat_func()
    local got1 = concat("test1\ntes")
    local got2 = concat("t2\n")
    local got3 = concat("")

    assert.equal("test1\n", got1)
    assert.equal("test2\n", got2)
    assert.equal("", got3)
  end)

  it("returns empty string if string has no newline", function()
    local concat = require("thetto.util.job.parse").concat_func()
    local got1 = concat("test")
    local got2 = concat("1\ntest2")
    local got3 = concat("")

    assert.equal("", got1)
    assert.equal("test1\n", got2)
    assert.equal("test2", got3)
  end)

  it("returns all string if empty string is inputted", function()
    local concat = require("thetto.util.job.parse").concat_func()
    local got1 = concat("test1\ntest2")
    local got2 = concat("")

    assert.equal("test1\n", got1)
    assert.equal("test2", got2)
  end)
end)
